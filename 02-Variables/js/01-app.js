// Variables Var (Sintaxis antigua), permite...

// Iniciarlizar una variable con un valor
var producto = "Monitor de 24 pulgadas";

//Las variables se puede reasignar
producto='Monitor de 19 pulgadas';

// JavaScript es un lenguaje de tipo dinamico, no se especifica tipo de dato
producto=20;
console.log(producto);

// Inicializar una variable sin valor, e inicializarlo después
var disponible;
disponible=true;
disponible=false;

// Inicializar múltiples variables (existen dos formas)
    // Primera:
    // var categoria='computadoras';
    // var marca="marca famosa";
    // var calificación=5;

    // Segunda:
    var categoria='computadoras',
        marca="marca famosa",
        calificación=5;
