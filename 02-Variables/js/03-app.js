// Variable const (Sintaxis moderna)

// Diferencias entre let y const
const producto='tablet';

// Primera: el valor de const no puede ser reaccionado
producto="monitor";
console.log(producto);

// Segunda: las variables const deben inicializarse siempre con un valor
const precio=20;
console.log(precio);
