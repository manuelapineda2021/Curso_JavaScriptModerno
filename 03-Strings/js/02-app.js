// MÉTODOS PARA LOS STRINGS
const producto='Monitor 20 pulgadas';
console.log(producto);

// Cantidad de letras de la cadena de texto
console.log(producto.length);

// Contiene una palabra la cadena de texto (resultado = posición)
console.log(producto.indexOf('pulgadas'));

// Contiene una palabra la cadena de texto (resultado= false o true)
console.log(producto.includes('Monitor'));