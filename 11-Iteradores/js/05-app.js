// Do While Loop: se ejecuta al menos una vez, y después verifica la condición
    // ya sea que se cumpla o no, al menos se va a ejecutar una vez

let i = 0; //Inicializo

do{
    console.log(`Número ${i}`);
    i++; //Incremento
    
}while(i < 10); //Condición